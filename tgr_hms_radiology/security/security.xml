<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">

        <!-- Radiology Req Rules-->
        <record id="tgr_radiology.all_hms_radiology_request_rule" model="ir.rule">
            <field name="groups" eval="[(4, ref('tgr_hms.group_hms_jr_doctor'))]"/>
        </record>

        <!-- Radiology Result Rules-->
        <record id="tgr_radiology.all_patient_radiology_test_rule" model="ir.rule">
            <field name="groups" eval="[(4, ref('tgr_hms.group_hms_jr_doctor'))]"/>
        </record>

    </data>
</odoo>
